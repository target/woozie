<?xml version="1.0" encoding="UTF-8" ?>
<workflow-app name="${JOB_NAME}" xmlns="uri:oozie:workflow:0.4">

    <global>
        <job-tracker>${jobTracker}</job-tracker>
        <name-node>${nameNode}</name-node>
        <configuration>
            <property>
                <name>mapreduce.job.queuename</name>
                <value>${queueName}</value>
            </property>
        </configuration>
    </global>

    <credentials>
        <credential name='hcat_creds' type='hcat'>
            <property>
                <name>hcat.metastore.uri</name>
                <value>${metastore_uri}</value>
            </property>
            <property>
                <name>hcat.metastore.principal</name>
                <value>${metastore_principal}</value>
            </property>
         </credential>
    </credentials>

    <start to="Config"/>

    <fork name="Fork1">
        <path start="Parallel1"/>
        <path start="Parallel2"/>
    </fork>

    <join name="Join1" to="Join2" />
    
    <action name="Action1" retry-max="1" retry-interval="1">
        <shell xmlns="uri:oozie:shell-action:0.3">
            <exec>config.sh</exec>
            <file>config.sh</file>
            <file>config.py</file>
            <capture-output/>
        </shell>
        <ok to="ActionOk"/>
        <error to="ActionError"/>
    </action>

    <decision name="Decision1">
        <switch>
            <!-- Run modeler workflow if first week of month-->
            <case to="DecisionCase">
                ${wf:actionData('Config')['FIRST_WEEK_OF_MONTH'] eq 'True' ||
                PROMOMASTER_RUN_MODELER eq 'TRUE'}
            </case>
            <default to="DecisionDefault"/>
        </switch>
    </decision>

    <kill name="KillAction">
        <message>Workflow failed, error message[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>

    <end name="End"/>

</workflow-app>
